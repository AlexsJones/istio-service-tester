apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: beta
spec:
  hosts:
    - beta
  gateways:
    - {{ .Values.gateway }}
  tcp:
    - route:
        - destination:
            host: beta
            port:
              number: 9001
